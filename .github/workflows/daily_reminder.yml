name: Daily Report Reminder (Mon-Sat)

on:
  schedule:
    - cron: '30 10 * * 1-6'   # 北京时间 18:30，周一到周六
  workflow_dispatch:

jobs:
  send-reminder:
    runs-on: ubuntu-latest
    steps:
      - name: Send Beautiful Markdown Reminder
        env:
          WECHAT_KEY: ${{ secrets.WECHAT_BOT_KEY }}
        run: |
          WEEKDAY=$(date '+%u')
          case $WEEKDAY in
            1) CN_DAY="周一" ;;
            2) CN_DAY="周二" ;;
            3) CN_DAY="周三" ;;
            4) CN_DAY="周四" ;;
            5) CN_DAY="周五" ;;
            6) CN_DAY="周六" ;;
          esac
          DATE_STR=$(date '+%m月%d日')
          URL="https://byjyedu.yuque.com/org-wiki-byjyedu-bxo6fa/bmqx33/gz2ixgatwha0u07k"

          curl "https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=$WECHAT_KEY" \
            -H 'Content-Type: application/json' \
            -d '{
              "msgtype": "markdown",
              "markdown": {
                "content": "#### 📅 <font color=\"info\">日报提交提醒</font> \n\n> **时间**：'$DATE_STR'（'$CN_DAY'）· <font color=\"warning\">19:00 截止</font>\n> **要求**：请完整填写今日工作内容\n\n<a href=\"'$URL'\">✅ 立即填写日报</a>\n\n@all"
              }
            }'
